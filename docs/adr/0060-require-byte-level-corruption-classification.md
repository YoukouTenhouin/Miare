# Require byte-level corruption classification

Qualification mutates every format layer and Protected unit role across fixed representative fixtures for all four encryption/compression combinations, including truncation, relocation, replay, substitution, compressed-frame damage, and cross-database or cross-Blob swaps. Passing requires exact public classification—visible compatibility or key/suite rejection, encrypted-slot `AuthenticationFailed`, permitted incomplete-publication fallback, or committed `Corrupt`—plus correct handle transitions, sentinel proof that no unchecked decoded content escapes, and no crash, hang, overread, unbounded allocation, or sensitive diagnostics; whole-file rollback remains intentionally undetected.
